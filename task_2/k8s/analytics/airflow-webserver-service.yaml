apiVersion: v1
kind: Service
metadata:
  name: airflow-webserver
  namespace: task2-analytics
  labels:
    app: airflow-webserver
spec:
  type: ClusterIP
  ports:
    - port: 8080
      targetPort: 8080
      name: http
  selector:
    app: airflow-webserver
