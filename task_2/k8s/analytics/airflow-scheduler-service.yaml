apiVersion: v1
kind: Service
metadata:
  name: airflow-scheduler
  namespace: task2-analytics
  labels:
    app: airflow-scheduler
spec:
  type: ClusterIP
  ports:
    - port: 8793
      targetPort: 8793
      name: log-server
  selector:
    app: airflow-scheduler
