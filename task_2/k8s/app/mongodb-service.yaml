apiVersion: v1
kind: Service
metadata:
  name: mongodb
  namespace: task2-app
  labels:
    app: mongodb
spec:
  type: LoadBalancer
  ports:
    - port: 27017
      targetPort: 27017
      name: mongodb
  selector:
    app: mongodb
