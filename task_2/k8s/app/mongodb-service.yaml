apiVersion: v1
kind: Service
metadata:
  name: mongodb
  namespace: task2-app
  labels:
    app: mongodb
spec:
  type: NodePort
  ports:
    - port: 27017
      targetPort: 27017
      nodePort: 30917
      name: mongodb
  selector:
    app: mongodb
