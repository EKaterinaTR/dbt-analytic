apiVersion: v1
kind: Service
metadata:
  name: sensor-service
  namespace: task2-app
  labels:
    app: sensor-service
spec:
  type: ClusterIP
  ports:
    - port: 8000
      targetPort: 8000
      name: http
  selector:
    app: sensor-service
