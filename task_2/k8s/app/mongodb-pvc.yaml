apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: mongodb-data
  namespace: task2-app
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: fast.ru-7a
  resources:
    requests:
      storage: 2Gi
